<UserControl x:Class="GuiPZ.MVVM.View.RegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GuiPZ.MVVM.View"
             mc:Ignorable="d"
             d:DesignHeight="670" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Button Width="30" Height="30" Grid.Row="0"
                Background="Coral"
                Content="bruh"
                Command="{Binding ProfilesViewCommand}"/>
        
        <StackPanel Grid.Row="1">
            <TextBox Width="100"/>
            <ListBox ItemsSource="{Binding DataLol}"/>
        </StackPanel>
    </Grid>
</UserControl>
