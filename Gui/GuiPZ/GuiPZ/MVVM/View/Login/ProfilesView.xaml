<UserControl x:Class="GuiPZ.MVVM.View.ProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GuiPZ.MVVM.View"
             mc:Ignorable="d"
             d:DesignHeight="670" d:DesignWidth="600">
    <Grid>
        <StackPanel>
            <Button Width="300" Height="300"
                    Background="Aqua"
                    Content="loluwa"
                    Command="{Binding NavMainCommand}"/>
        
            <Button Width="300" Height="300"
                    Background="Gray"
                    Content="bruhstersko"
                    Command="{Binding RegistrationViewCommand}"/>
        </StackPanel>
        
    </Grid>
</UserControl>
